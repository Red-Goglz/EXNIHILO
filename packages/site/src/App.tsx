// App.tsx is no longer used — routing is handled in main.tsx via createBrowserRouter.
// Kept as empty export to avoid import errors if referenced elsewhere.
export default function App() {
  return null;
}
