{
  "name": "exnihilo-indexer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev --schema exnihilo_avax",
    "start": "ponder start --schema exnihilo_avax",
    "build": "echo 'No build step needed — ponder start handles everything'",
    "codegen": "ponder codegen"
  },
  "dependencies": {
    "@exnihilio/abis": "*",
    "hono": "^4.12.14",
    "ponder": "^1.0.0",
    "viem": "^2.48.1"
  }
}
